import React from 'react';
import Header from "components/Header";

const Home = (props) => {
    return (
        <>
            <Header />
            <div>HOME</div>
        </>
    );
}

export default Home;